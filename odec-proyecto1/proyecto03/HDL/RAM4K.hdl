CHIP RAM4K {
    IN in[16], load, address[12];
    OUT out[16];

    PARTS:
    DMux8Way(in=load, sel=address[9..11], a=load0, b=load1, c=load2, d=load3, e=load4, f=load5, g=load6, h=load7);
    RAM512(in=in, load=load0, address=address[0..8], out=outr0);
    RAM512(in=in, load=load1, address=address[0..8], out=outr1);
    RAM512(in=in, load=load2, address=address[0..8], out=outr2);
    RAM512(in=in, load=load3, address=address[0..8], out=outr3);
    RAM512(in=in, load=load4, address=address[0..8], out=outr4);
    RAM512(in=in, load=load5, address=address[0..8], out=outr5);
    RAM512(in=in, load=load6, address=address[0..8], out=outr6);
    RAM512(in=in, load=load7, address=address[0..8], out=outr7);
    Mux8Way16(a=outr0, b=outr1, c=outr2, d=outr3, e=outr4, f=outr5, g=outr6, h=outr7, sel=address[9..11], out=out);
}